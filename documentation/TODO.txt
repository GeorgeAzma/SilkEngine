- Implement dynamic multi texturing
- Multiple render passes
- SPIRV, reflections and stuff
- Support more instances, by making vertex buffer resizable
- Fix coordinate system (Kinda already did)
- Better material system
- Font rendering
- Descriptor allocator
- Caching (Graphics Pipeline etc.)
- Editable shader defines
- Shader includes
- 4 component image converter shader
- OpenAL and audio streaming
- Video (mp4 etc.)
- Create system which handles buffer creation for multiple frames 
(perhaps static function like Graphics::createBuffer())
- Sort meshes and materials and render with minimal binds
- Fix dirty swap chain code and render pass code
- Seperate dispatchers for related events and create getters for them
- Configurable graphics settings (preferably in file format like yaml) 